# 「小滴答」桌面每日提醒小程序 · 策划书

> 版本 v0.4（2026-09-13，安全与稳定专项修订）· 工作名「小滴答」仅占位，随时可改
> 平台：Windows 10/11 · 技术栈：Electron · 纯本地应用，无账号无联网

## 1. 一句话定位

一个常驻桌面的半透明小卡片：显示实时时间和你今天的提醒任务，到点自动弹出提醒，完成任务有解压动画。轻量、克制、好看。

## 2. 核心设计原则

1. **稳定与安全优先**：数据不丢、提醒不漏、绝不双开、绝不重复弹窗。宁可功能少，不可不稳定。
2. **添加成本极低，但不设上限**：90% 情况靠点选一键完成，每个点选步骤都留自定义入口，特殊需求不被卡死。
3. **不喧宾夺主**：小组件平时半透明安静待着，动画一律 1 秒内结束，不挡当前工作。
4. **解压而非施压**：完成反馈是奖励感（彩屑/气泡），不搞红点焦虑、不做逾期惩罚。
5. **数据永远在本机**：一个 JSON 文件管所有数据，随时可导出备份，全程零联网。

## 3. 功能规格

### 3.1 常驻小组件（主窗口）

- **半透明**：设置内提供透明度滑块（30%~100%）；鼠标悬停时自动变清晰便于阅读操作，移开恢复基准值。
- **锁定（置顶）**：置顶开关。开启后小组件始终浮在所有窗口上方（即「锁定到当前页面」），关闭则可被遮挡。默认开启。
- **缩放与位置**：可自由拖动边缘缩放（设最小/最大尺寸），位置和大小自动记忆。另预置「标准 / 迷你」两档：迷你模式缩为一个小圆球，只显示时钟和未完成数，点击恢复。
- **实时时钟**：顶部显示当前时间与日期（含星期）。
- **无边框**：无系统标题栏，用自定义迷你工具条代替：置顶锁、设置、迷你模式。
- **任务列表秩序**：已完成任务自动沉底灰显（划线），未完成的按触发时间排序。

布局草案（紧凑态）：

```
┌────────────────────────────┐
│  14:32   9月13日 周日   🔒 ⚙ │   ← 时钟日期 + 工具条(置顶/设置/迷你)
│ ──────────────────────────  │
│  ✓ 喝水 · 10:00             │
│  ○ 起身活动 · 11:00     🔥3  │   ← 点○即完成；🔥=连击徽章
│  ○ 休息眼睛 · 12:00         │
│ ──────────────────────────  │
│  ＋添加    🍅 25:00  ▶      │   ← 添加入口 + 番茄钟迷你条
└────────────────────────────┘
```

### 3.2 提醒任务

**数据模型**（每条任务）：名称、触发时间、重复规则、开关（可临时停用不删除）。

**添加流程：模板库 + 点选，每个点选步骤都是「快捷选项 + 自定义」双层结构**

1. **选模板**：点「＋添加」→ 模板九宫格 = 预设模板（喝水、休息眼睛、起身活动、午饭、午休、下班收尾、吃药）+ **你自己存过的模板** + 「自定义」入口。任何任务保存时可勾选「存为模板」，模板库越用越顺手。
2. **定名称**：点选模板后名称自动带入，但**名称随时可改**——点一下就地编辑；选「自定义」则直接输入（限 50 字符）。
3. **选时间**：常用时段快捷按钮（9:00 / 12:00 / 14:00 / 18:00）+ ±15 分钟步进微调 + **「自定义」**：时间滚轮/直接键入，精确到任意分钟。
4. **选重复**：预设单选（仅一次 / 每天 / 工作日 / 每 1 小时）+ **「自定义」**：周几自由多选（如周一三五）、任意间隔（N 分钟或 N 小时）。

整个添加流程默认全程零键盘；想精细控制时，每一步都能展开自定义。

**任务管理**：右键/长按任务行 → 编辑 / 稍后提醒 / 停用 / 删除；编辑复用添加表单（已填充当前值）。

**规则细节**：

- 间隔类任务必须设**生效时间段**（默认 09:00–22:00），避免半夜弹「喝水」；按固定时刻表触发（从起始时间起算），完成不改变下一次触发时间。
- 仅一次任务触发并处理后自动归档，不再占列表。
- 「稍后再提醒」默认 5 分钟（设置里可改档位：5/10/15/30 分钟）。

### 3.3 提醒弹窗

- 到点从**屏幕右下角**滑出，与小组件同主题同风格；**不抢焦点**（不打断打字/游戏）。
- 内容：任务名 + 当前时间，按钮：`✓ 完成` / `稍后再提醒` / `今天跳过`。
- 弹窗不自动消失，需用户明确处理；同刻多条任务合并为一张卡列表。不同时间的多条未处理弹窗在右下角垂直堆叠，超过 3 条折叠为「+N」，点击展开；也可以直接在小组件列表里处理，弹窗只是入口之一。
- 「今天跳过」仅对当次生效（明日照常提醒）。
- **每条提醒带去重标记**（任务 id + 触发时刻），无论正常调度、贪睡还是唤醒补弹，同一时刻最多只弹一次。
- **降级兜底**：不做全屏检测（Windows 下需原生依赖且易误判）。弹窗发出后 **2 分钟**仍未处理，自动补发一条 Windows 系统通知；因系统休眠错过的提醒在唤醒后补弹。两种情况都不重复轰炸。

### 3.4 完成动画（三选一随机播放）

每次完成任务从下面三种里随机播一种，全部 ≤1 秒、不阻塞后续点击：

| 动画 | 表现 | 气质 |
|---|---|---|
| 对勾 + 小把彩屑 | 对勾划入，十几粒彩纸从任务条迸出散落 | 活泼 |
| 气泡破裂 + 涟漪 | 任务条像水珠被按瘪再弹开消散，一圈涟漪扩散 | 安静/解压 |
| 划线淡出 | 任务条翻虚线划掉后淡出 | 沉稳 |

### 3.5 主题系统（活泼 / 沉稳 / 安静）

用 CSS 变量实现，切换即时生效、无需重启。三个主题不只换色，整体气质联动：

| 维度 | 活泼 | 沉稳 | 安静 |
|---|---|---|---|
| 主色 | 珊瑚橙 / 明黄点缀 | 藏蓝灰 / 墨绿 | 雾灰 / 淡青 |
| 动效 | 弹性曲线、幅度大 | 平缓利落 | 柔和缓慢淡入 |
| 提示音 | 清脆风铃 | 温和木琴 | 水滴 |
| 细节 | 大圆角、圆润字重 | 小圆角、字重利落 | 大圆角、细字重 |

### 3.6 番茄钟（附加）

小组件内嵌迷你条：一键开始默认 25 分钟专注 + 5 分钟休息，时长可自定义；倒计时环显示进度，结束触发提示音 + 弹窗（同提醒弹窗样式，按钮为「开始休息 / 跳过」）。番茄钟独立于任务统计，不计入完成数与连击。

### 3.7 统计与连击（附加）

- 小组件上只放一个极简徽章：今日完成数 + 🔥 连续打卡天数。
- 点击徽章展开最近 30 天日历热力视图（完成越多颜色越深）。只有正反馈，没有断签惩罚展示。
- **连击定义**：当天完成 ≥1 条任务即打卡成功，连击 +1（在当天首次完成时结算）；「跳过」是中性操作，不计完成数、不打断连击。

### 3.8 提示音效（附加）

内置 3 套轻柔音色（风铃 / 木琴 / 水滴），随主题自动匹配、也可手动换；总开关独立于系统音量。完成任务的音效与提醒音效分开。

### 3.9 托盘与开机自启（附加）

- 关闭/隐藏小组件 = 缩到系统托盘继续运行；托盘菜单：显示/隐藏小组件、立即退出。
- 开机自启用 Electron 官方接口实现，设置里一个开关，默认关闭（尊重用户选择）。

### 3.10 设置面板（⚙ 汇总）

主题 / 透明度滑块 / 提示音（开关 + 音色）/ 贪睡档位 / 番茄钟时长 / 开机自启 / 导出与导入备份 / 关于（含版本号）。

## 4. 技术方案

- **框架**：Electron + 原生 HTML/CSS/JS（不上框架，界面简单，纯 DOM 足够，减少依赖）。依赖锁版本（package-lock），Electron 用稳定版。
- **窗口**：主窗口 `transparent + frameless + resizable`，置顶用 `alwaysOnTop`（默认级别，不抢 screen-saver 级）；提醒弹窗为**单个复用窗口**（不反复新建），以 `showInactive`（不抢焦点）方式出现。
- **调度**：调度器放主进程（不受渲染层计时器节流影响），每分钟对齐检查一次任务表；监听系统睡眠唤醒事件，唤醒后补弹错过的提醒。
- **时钟显示**：渲染层每秒 tick 时直接读取系统时间（不自增秒数），即使被 Chromium 后台节流也能自动校准，不走时不准。
- **跨天处理**：每日任务在日期变更时自动重置为未完成；跨天瞬间小组件无感刷新。
- **越界保护**：启动时检测小组件是否落在可见显示区内，不在则拉回主屏（应对拔掉外接显示器的情况）。
- **数据**：单 JSON 文件存于 Electron 标准用户数据目录（用户目录内，无需管理员权限）；写盘防抖合并（500ms），退出前强制落盘。详见第 5 章完整性策略。
- **打包**：electron-builder 出 NSIS 安装包，同时支持绿色便携版 exe。

## 5. 安全与稳定基线（最高优先级）

**安全**

- Electron 安全开关全开：`contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`；渲染进程只通过 `contextBridge` 暴露的白名单 API（增删改查任务、读写设置等十来个方法）与主进程通信，且主进程侧对每个参数做类型校验。
- 只加载本地文件（`loadFile`），绝不加载任何远程 URL；零网络请求——字体用系统字体、音效本地打包，不引任何 CDN/远程资源。
- 所有用户自定义文本（任务名、模板名）用 `textContent` 插入 DOM，禁止 innerHTML 拼接，杜绝注入；文本限长（50 字符）。
- **导入备份必须校验**：检查 JSON 结构、字段类型与取值范围（时间格式、重复规则枚举等），不合法即拒绝并明确提示，绝不静默写入主数据。

**数据不丢**

- 三重防护：① 原子写（先写临时文件再替换）；② 每次成功写盘后轮换保留上一版 `.bak`；③ 启动时校验数据文件，损坏则自动从 `.bak` 恢复并提示用户；备份也损坏则进入空数据模式并明确告知，绝不白屏或崩溃。

**进程稳定**

- **单实例锁**：第二个实例启动时立即退出并唤起已有窗口，杜绝双开导致的重复提醒。
- 渲染进程崩溃（`render-process-gone`）自动重载窗口，状态从数据文件恢复。
- 主进程未捕获异常写入滚动日志（限制大小，自动清理旧日志），退出前强制落盘数据。
- 动画只用 CSS `transform/opacity`（合成层属性），不引发重排，1 秒动画不卡顿。

## 6. 数据结构（示意）

```json
{
  "settings": {
    "theme": "lively", "opacity": 0.75, "pinned": true,
    "sound": true, "soundPack": "windchime",
    "widgetBounds": { "x": 100, "y": 100, "w": 300, "h": 420 },
    "launchAtLogin": false, "snoozeMinutes": 5,
    "pomodoro": { "focus": 25, "break": 5 }
  },
  "customTemplates": [
    { "title": "给花浇水", "time": "20:00", "repeat": { "type": "weekly", "weekdays": [6] } }
  ],
  "tasks": [
    { "id": "uuid", "title": "休息眼睛", "time": "12:00",
      "repeat": { "type": "daily" }, "enabled": true },
    { "id": "uuid", "title": "喝水", "time": "09:00",
      "repeat": { "type": "interval", "minutes": 120, "activeWindow": ["09:00", "22:00"] }, "enabled": true }
  ],
  "stats": { "days": { "2026-09-13": 4 }, "streak": 3 }
}
```

## 7. v1 明确不做（防蔓延）

云同步/账号、多设备、农历与节日倒数、桌面便签、鼠标点击穿透、全屏检测、自动更新、多语言、代码签名证书。以上进「以后再说」清单。

## 8. 里程碑（每步带验收标准）

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| M1 骨架 | 透明置顶小组件 + 时钟 + 任务增删改 + 到点弹窗 + 贪睡 + 单实例锁 + 原子写 | 设一条 1 分钟后的提醒，弹窗准时出现，完成/贪睡按钮可用；重复启动第二个实例被阻止；重启后任务与窗口位置不丢 |
| M2 质感 | 三主题、三种完成动画随机、模板库点选添加、透明度滑块 + 悬停、音效 | 切换主题即时生效；用模板添加任务全程零键盘输入，且模板/名称/时间/重复四个步骤均能找到自定义入口 |
| M3 附加 | 番茄钟、统计连击、托盘 + 开机自启 | 完成任务后连击数 +1 且持久化；关窗后托盘仍在运行 |
| M4 收尾 | 边界情况（睡眠唤醒补弹、跨天重置、越界拉回、多显示器 DPI）+ 数据损坏恢复 + 打包 | 休眠唤醒后错过提醒自动补弹且不重复；拔掉外接显示器后启动，小组件自动回到可见区域；手动改坏数据文件后启动能自动从备份恢复；打包出的 exe 在干净机器可安装运行 |

## 9. 风险与对策

- **Electron 内存占用约 100~200MB**：已知取舍，换来最好的界面表现力；若日后在意可平迁 Tauri，界面代码可复用。
- **数据文件损坏丢数据**：原子写 + `.bak` 轮换 + 启动自动恢复三重防护（第 5 章）。
- **全屏应用盖住弹窗**：不做全屏检测，用「2 分钟未处理补发系统通知」兜底（3.3），同时覆盖用户离开的情况。
- **睡眠/休眠错过提醒**：唤醒后补弹，去重标记防止与正常调度重复（3.3）。
- **后台时钟被节流走时不准**：tick 直接读系统时间自动校准（4）。
- **拔掉显示器后小组件留在屏外**：启动时越界检测拉回主屏（4）。
- **未签名 exe 触发 SmartScreen 警告**：个人项目暂不购买签名证书，属正常现象；「关于」里写明首次运行的「仍要运行」步骤，有需要后再上证书。
- **无边框窗口在 Windows 的边缘缩放手感**：M1 优先验证，不行就退回「标准/迷你两档尺寸」方案。

## 10. 待你拍板的开放问题

1. 应用名字（现用占位「小滴答」）。
2. 默认模板清单是否要增减（当前拟：喝水、休息眼睛、起身活动、午饭、午休、下班收尾、吃药）。
